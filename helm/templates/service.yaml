apiVersion: v1
kind: Service
metadata:
  name: {{ include "waltz.fullname" . }}
  labels:
    {{- include "waltz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "waltz.selectorLabels" . | nindent 4 }}
